// ChatPanel removed — plugin fields moved to Inbox UI.
describe('ChatPanel (removed)', () => {
  it('no longer exists — plugin fields are configured through the Inbox UI', () => {
    expect(true).toBe(true)
  })
})
